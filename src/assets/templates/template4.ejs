<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Fiche Article - Version <%= version %></title>
    <style>
        body {
            padding: 1cm;
            margin: 0.1cm;
            color: #555555;
            background: #ffffff;
            font-family: Arial, sans-serif;
            font-size: 14px;
            position: relative;
            min-height: 100vh;
        }

        .invoice-1 {
            max-width: 1625px;
            margin: 0 auto;
        }

        .invoice-headar {
            margin-bottom: 20px;
            position: relative;
        }

        .invoice-logo img {
            width: 100px;
            height: auto;
        }

        .invoice-id {
            text-align: right;
        }

        .invoice-id h1 {
            font-size: 24px;
            color: #25306e;
            margin-bottom: 10px;
        }

        .invoice-id p {
            margin: 5px 0;
            color: #555555;
        }

        .invoice-top {
            margin-bottom: 20px;
        }

        .invoice-number h5 {
            font-size: 16px;
            color: #25306e;
            margin-bottom: 10px;
        }

        .invoice-number h4 {
            font-size: 18px;
            color: #353535;
            margin-bottom: 10px;
        }

        .invoice-number p {
            margin: 5px 0;
            color: #555555;
        }

        .signature-section {
            position: absolute;
            bottom: 20px;
            right: 20px;
            text-align: right;
        }

        .signature-section strong {
            font-size: 16px;
            color: #25306e;
        }
    </style>
</head>
<body>
    <div class="invoice-1 invoice-content">
        <div class="container-fluid">
            <div class="row">
                <div class="col-lg-12">
                    <div class="invoice-inner clearfix">
                        <div class="invoice-info clearfix" id="invoice_wrapper">
                            <!-- QR Code en haut à droite - Optionnel -->
                            <% if (article.qrCode && article.qrCode !== '') { %>
                                <div class="qr-code-top">
                                    <img src="<%= article.qrCode %>" alt="QR Code">
                                </div>
                            <% } %>

                            <!-- Header -->
                            <div class="invoice-headar">
                                <div class="row g-0">
                                    <div class="col-sm-6">
                                        <div class="invoice-logo">
                                            <div class="logo">
                                                <img src="<%= logo %>" alt="logo">
                                            </div>
                                        </div>
                                    </div>
                                    <div class="col-sm-6 invoice-id">
                                        <div class="info">
                                            <h1>Fiche Article - Version <%= version %></h1>
                                            <p><strong>Date :</strong> <%= new Date(date).toLocaleDateString('fr-FR') %></p>
                                        </div>
                                    </div>
                                </div>
                            </div>

                            <!-- Article Details -->
                            <div class="invoice-top">
                                <div class="row">
                                    <div class="col-sm-6">
                                        <div class="invoice-number mb-30">
                                            <h5>Détails de l'article</h5>
                                            <p><strong>Titre :</strong> <%= article.title || 'Non spécifié' %></p>
                                            <p><strong>Description :</strong> <%= article.description || 'Aucune description' %></p>
                                            <p><strong>Référence :</strong> <%= article.reference %></p>
                                            <p><strong>Prix unitaire :</strong> <%= article.unitPrice ? article.unitPrice.toFixed(2) + ' €' : 'Non spécifié' %></p>
                                            <p><strong>Quantité en stock :</strong> <%= article.quantityInStock %></p>
                                            <p><strong>Statut :</strong> <%= article.status %></p>
                                            <p><strong>Version :</strong> <%= article.version %></p>
                                            <% if (article.notes) { %>
                                                <p><strong>Notes :</strong> <%= article.notes %></p>
                                            <% } %>
                                            <% if (article.justificatifFileName) { %>
                                                <p><strong>Justificatif :</strong> <%= article.justificatifFileName %></p>
                                            <% } %>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <!-- Signature Section -->
        <div class="signature-section">
            <strong>Cachet & Signature</strong>
        </div>
    </div>
</body>
</html>