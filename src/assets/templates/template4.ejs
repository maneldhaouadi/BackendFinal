<!DOCTYPE html>
<html lang="fr">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Fiche Article - Version <%= version %></title>
    <style>
        body {
            padding: 1cm;
            margin: 0.1cm;
            color: #555555;
            background: #ffffff;
            font-family: Arial, sans-serif;
            font-size: 14px;
            position: relative; /* Pour positionner la signature en bas */
            min-height: 100vh; /* Pour s'assurer que le corps prend toute la hauteur */
        }

        .invoice-1 {
            max-width: 1625px;
            margin: 0 auto;
        }

        .invoice-headar {
            margin-bottom: 20px;
            position: relative; /* Pour positionner le code QR en haut à droite */
        }

        .invoice-logo img {
            width: 100px;
            height: auto;
        }

        .invoice-id {
            text-align: right;
        }

        .invoice-id h1 {
            font-size: 24px;
            color: #25306e;
            margin-bottom: 10px;
        }

        .invoice-id p {
            margin: 5px 0;
            color: #555555;
        }

        .qr-code-top {
            position: absolute; /* Position absolue pour placer en haut à droite */
            top: 0;
            right: 0;
            text-align: right;
        }

        .qr-code-top img {
            max-width: 80px; /* Taille réduite du code QR */
            height: auto;
        }

        .invoice-top {
            margin-bottom: 20px;
        }

        .invoice-number h5 {
            font-size: 16px;
            color: #25306e;
            margin-bottom: 10px;
        }

        .invoice-number h4 {
            font-size: 18px;
            color: #353535;
            margin-bottom: 10px;
        }

        .invoice-number p {
            margin: 5px 0;
            color: #555555;
        }

        .signature-section {
            position: absolute; /* Position absolue pour placer en bas */
            bottom: 20px; /* Position en bas */
            right: 20px; /* Alignement à droite */
            text-align: right; /* Texte aligné à droite */
        }

        .signature-section strong {
            font-size: 16px;
            color: #25306e;
        }
    </style>
</head>

<body>
    <div class="invoice-1 invoice-content">
        <div class="container-fluid">
            <div class="row">
                <div class="col-lg-12">
                    <div class="invoice-inner clearfix">
                        <div class="invoice-info clearfix" id="invoice_wrapper">
                            <!-- QR Code en haut à droite -->
                            <div class="qr-code-top">
                                <% if (article.qrCode) { %>
                                    <img src="<%= article.qrCode %>" alt="QR Code">
                                <% } %>
                            </div>

                            <!-- Header -->
                            <div class="invoice-headar">
                                <div class="row g-0">
                                    <div class="col-sm-6">
                                        <div class="invoice-logo">
                                            <div class="logo">
                                                <img src="<%= logo %>" alt="logo">
                                            </div>
                                        </div>
                                    </div>
                                    <div class="col-sm-6 invoice-id">
                                        <div class="info">
                                            <h1>Fiche Article - Version <%= version %></h1>
                                            <p><strong>Date :</strong> <%= date %></p>
                                        </div>
                                    </div>
                                </div>
                            </div>

                            <!-- Article Details -->
                            <div class="invoice-top">
                                <div class="row">
                                    <div class="col-sm-6">
                                        <div class="invoice-number mb-30">
                                            <h5>Détails de l'article</h5>
                                            <p><strong>Titre :</strong> <%= article.title %></p>
                                            <p><strong>Description :</strong> <%= article.description %></p>
                                            <p><strong>Catégorie :</strong> <%= article.category %></p>
                                            <p><strong>Sous-catégorie :</strong> <%= article.subCategory %></p>
                                            <p><strong>Prix d'achat :</strong> <%= article.purchasePrice %> €</p>
                                            <p><strong>Prix de vente :</strong> <%= article.salePrice %> €</p>
                                            <p><strong>Quantité en stock :</strong> <%= article.quantityInStock %></p>
                                            <p><strong>Code-barres :</strong> <%= article.barcode %></p>
                                            <p><strong>Statut :</strong> <%= article.status %></p>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <!-- Signature Section -->
        <div class="signature-section">
            <strong>Cachet & Signature</strong>
        </div>
    </div>
</body>

</html>