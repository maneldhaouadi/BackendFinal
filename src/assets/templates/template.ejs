<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <title><%= data.meta.type %> N°<%= data.quotation.sequential %></title>
  <style>
    body { font-family: Arial, sans-serif; margin: 20px; }
    .dynamic-field { background-color: #f5f5f5; padding: 2px 5px; border-radius: 3px; }
    table { width: 100%; border-collapse: collapse; }
    th, td { border: 1px solid #ddd; padding: 8px; text-align: left; }
  </style>
</head>
<body>
  <h1><%= data.meta.type %> N°<%= data.quotation.sequential %></h1>
  
  <% if (data.quotation.firm) { %>
    <div class="firm-info">
      <h3><%= data.quotation.firm.name %></h3>
      <% if (data.quotation.firm.address) { %>
        <p><%= data.quotation.firm.address %></p>
      <% } %>
    </div>
  <% } %>

  <div class="document-info">
    <p>Date: <%= data.quotation.date %></p>
    <p>Objet: <%= data.quotation.object %></p>
  </div>

  <!-- Vous pouvez ajouter plus de sections selon vos besoins -->
</body>
</html>